[phases.setup]
nixPkgs = [
  "chromium",
  "chromium-driver",
  "xvfb-run"
]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install chromium",
  "playwright install-deps chromium"
]
